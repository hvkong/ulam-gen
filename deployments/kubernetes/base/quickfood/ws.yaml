# service.name: 'ws'
# service.component: 'ws' (defined in app code)
apiVersion: apps/v1
kind: Deployment
metadata:
  # unique name for this deployment resource
  name: quickfood-ws
  labels: &wsLabels # application name: groups all related resources
    app.k8s.io/name: quickfood
    # component type: defines the role of this deployment
    app.kubernetes.io/component: service
    # instance id: used for service discovery and telemetry (service.name/service_name attrs)
    app.kubernetes.io/instance: ws
spec:
  replicas: 1
  selector:
    matchLabels: *wsLabels
  template:
    metadata:
      labels: *wsLabels
    spec:
      containers:
        - name: quickfood
          image: ghcr.io/grafana/quickfood-local:0.15.21
          ports:
            - name: http
              containerPort: 3333
          envFrom:
            - configMapRef:
                name: quickfood-env-common
            - configMapRef:
                name: quickfood-env
                optional: true
          env:
            - name: QUICKFOOD_ENABLE_WS_SERVICE
              value: "1"
            - name: QUICKFOOD_OTEL_SERVICE_INSTANCE_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: QUICKFOOD_OTEL_SERVICE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.labels['app.kubernetes.io/instance']
---
# Configures value for QUICKFOOD_WS_ENDPOINT (set in kustomization.yaml)
apiVersion: v1
kind: Service
metadata:
  name: quickfood-ws
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/instance: ws
  ports:
    - protocol: TCP
      port: 3333
      targetPort: 3333
