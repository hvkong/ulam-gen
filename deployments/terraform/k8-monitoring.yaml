metrics:
  enabled: true
  kepler:
    enabled: true

alloy-logs:
  alloy:
    mounts:
      dockercontainers: true

logs:
  enabled: true
  pod_logs:
    enabled: true
    # The Alloy pod in quickfood ns collects quickfood logs
    excludeNamespaces: [quickfood]

traces:
  enabled: false

profiles:
  enabled: false

kepler:
  enabled: true
